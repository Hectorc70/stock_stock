import 'package:flutter/material.dart';


class ShimmerSale extends StatelessWidget {
  const ShimmerSale({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}